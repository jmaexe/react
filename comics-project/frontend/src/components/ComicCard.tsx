import { Comic } from "../models/Comics";

type ComicCardProps = {
  comic: Comic;
};

const ComicCard = ({}: ComicCardProps) => {
  return (
    <>
      <p>ComicCard</p>
    </>
  );
};

export default ComicCard;
