import {View,Text, Image} from 'react-native';

type CastCardProps = {
data: Cast
}

const CastCard = ({data }: CastCardProps) => {
  return (
    <View>
        <Image source={{uri: }}/>
      <Text></Text>
    </View>
  );
};

export default CastCard;